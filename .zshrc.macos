# oh-my-zsh
export ZSH="$HOME/.oh-my-zsh"
source $ZSH/oh-my-zsh.sh

# Add brew packages to path
eval "$(/opt/homebrew/bin/brew shellenv)"


# Aliases
alias cls="clear"
alias ..="cd .."
alias vim="nvim"
alias vi="nvim"
alias whisper="uvx --from openai-whisper whisper"
alias lg="lazygit"

# bun completions
[ -s "/Users/aleks/.bun/_bun" ] && source "/Users/aleks/.bun/_bun"
# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"


export PNPM_HOME="/Users/aleks/Library/pnpm"
export PATH="$PNPM_HOME:$PATH"

export LESS="-F -X $LESS"

alias ls="eza --icons --group-directories-first --time-style=\"+%d.%m.%Y %H:%M\""

# FNM (Fast Node version Manager)
eval "$(fnm env --use-on-cd --shell zsh)"

# Zoxide (better cd)
eval "$(zoxide init zsh)"

# Starship prompt
eval "$(starship init zsh)"

# JJ source-control
source <(jj util completion zsh)

# Typst Completions
source <(typst completions zsh)

autoload -U compinit
compinit -i

# Added by `rbenv init` on Fri Oct 17 13:21:23 CEST 2025
eval "$(rbenv init - --no-rehash zsh)"
